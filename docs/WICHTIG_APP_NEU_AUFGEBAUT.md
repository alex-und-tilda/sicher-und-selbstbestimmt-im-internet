# Wichtig: app.js neu aufgebaut

Diese Version ersetzt die beschädigte gewachsene `app.js` durch eine stabile Minimal-Version.

Ziel:

```text
Die Seite muss wieder zuverlässig laden.
```

Nicht enthalten:

```text
keine Vorlesen-Funktion
kein Testbogen
keine automatische Rückmeldung
keine Lernstand-Speicherung
```

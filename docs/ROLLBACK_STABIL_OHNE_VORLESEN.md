# Rollback: stabile Version ohne neue Vorlese-Logik

Diese Version setzt auf die letzte stabile Fassung zurück.

Geändert:

```text
keine neue Vorlese-Funktion
keine Safe-Listen-Schaltfläche
keine Veränderung an Lernkarten-Templates
keine Veränderung an Quiz-Templates
Starttitel bleibt: Sicher und selbstbestimmt im Internet
```

Ziel:

```text
Die Seite soll zuerst wieder zuverlässig funktionieren.
Danach kann die Vorlesefunktion neu und kontrolliert geplant werden.
```
